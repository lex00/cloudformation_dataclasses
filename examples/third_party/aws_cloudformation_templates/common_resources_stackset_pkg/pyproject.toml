[project]
name = "common_resources_stackset_pkg"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cloudformation_dataclasses",
]

[project.scripts]
common_resources_stackset_pkg = "common_resources_stackset_pkg.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["common_resources_stackset_pkg"]

[tool.hatch.build.targets.sdist]
include = ["common_resources_stackset_pkg/**/*.py", "common_resources_stackset_pkg/resources/"]

[dependency-groups]
dev = [
    "mypy>=1.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

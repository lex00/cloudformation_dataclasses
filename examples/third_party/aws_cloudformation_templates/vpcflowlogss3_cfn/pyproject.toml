[project]
name = "vpcflowlogss3_cfn"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cloudformation_dataclasses",
]

[project.scripts]
vpcflowlogss3_cfn = "vpcflowlogss3_cfn.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["vpcflowlogss3_cfn"]

[tool.hatch.build.targets.sdist]
include = ["vpcflowlogss3_cfn/**/*.py", "vpcflowlogss3_cfn/resources/"]

[dependency-groups]
dev = [
    "mypy>=1.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

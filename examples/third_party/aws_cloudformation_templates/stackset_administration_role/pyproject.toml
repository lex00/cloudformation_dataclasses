[project]
name = "stackset_administration_role"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cloudformation_dataclasses",
]

[project.scripts]
stackset_administration_role = "stackset_administration_role.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["stackset_administration_role"]

[tool.hatch.build.targets.sdist]
include = ["stackset_administration_role/**/*.py", "stackset_administration_role/resources/"]

[dependency-groups]
dev = [
    "mypy>=1.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[project]
name = "private_service"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cloudformation_dataclasses",
]

[project.scripts]
private_service = "private_service.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["private_service"]

[tool.hatch.build.targets.sdist]
include = ["private_service/**/*.py"]

[dependency-groups]
dev = [
    "mypy>=1.0",
]

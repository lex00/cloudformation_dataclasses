[project]
name = "macro_4"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cloudformation_dataclasses",
]

[project.scripts]
macro_4 = "macro_4.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["macro_4"]

[tool.hatch.build.targets.sdist]
include = ["macro_4/**/*.py"]

[dependency-groups]
dev = [
    "mypy>=1.0",
]

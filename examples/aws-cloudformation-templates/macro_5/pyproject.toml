[project]
name = "macro_5"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cloudformation_dataclasses",
]

[project.scripts]
macro_5 = "macro_5.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["macro_5"]

[tool.hatch.build.targets.sdist]
include = ["macro_5/**/*.py"]

[dependency-groups]
dev = [
    "mypy>=1.0",
]

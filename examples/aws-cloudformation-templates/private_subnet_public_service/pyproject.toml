[project]
name = "private_subnet_public_service"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cloudformation_dataclasses",
]

[project.scripts]
private_subnet_public_service = "private_subnet_public_service.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["private_subnet_public_service"]

[tool.hatch.build.targets.sdist]
include = ["private_subnet_public_service/**/*.py", "private_subnet_public_service/resources/"]

[dependency-groups]
dev = [
    "mypy>=1.0",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

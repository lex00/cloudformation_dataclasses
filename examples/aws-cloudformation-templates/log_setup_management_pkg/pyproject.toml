[project]
name = "log_setup_management_pkg"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "cloudformation_dataclasses",
]

[project.scripts]
log_setup_management_pkg = "log_setup_management_pkg.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["log_setup_management_pkg"]

[tool.hatch.build.targets.sdist]
include = ["log_setup_management_pkg/**/*.py"]

[dependency-groups]
dev = [
    "mypy>=1.0",
]
